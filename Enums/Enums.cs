namespace project_RoleTopMVC.Enums
{
    public enum StatusPedido : uint
    {
        PEDENTE = 0,
        APROVADO = 1,
        REPROVADO = 2
    }

    public enum TiposUsuario : uint
    {
        ADMINISTRADOR = 0,
        CLIENTE = 1
    }
}